# 启动项目流程（Windows / PowerShell）

本文以仓库根目录下的 `amusement_stats/` 为主工程目录，整理从“准备环境”到“成功启动并访问页面”的完整流程，并说明每一步在系统里的作用与验证方法。

---

## 1. 项目启动需要什么

### 1.1 必备软件

- **Windows 10/11**
- **Python 3.12+**
  - 本项目依赖 `Django==6.0.4`，该版本要求 Python 3.12 及以上。
- （建议）**Python Launcher（py）**
  - 便于同时管理多版本 Python，执行 `py -3.12 ...`。

### 1.2 进入正确目录

该仓库的 Django 工程根目录是：

```powershell
cd \amusement_stats
```

你应当能看到以下文件/目录：

- `manage.py`：Django 管理命令入口
- `requirements.txt`：依赖清单
- `offline_packages/`：离线依赖 wheel（当前只包含部分包）
- `db.sqlite3`：默认 SQLite 数据库文件

---

## 2. 安装 Python 3.12（若已安装可跳过）

### 2.1 用 winget 安装（推荐）

```powershell
winget install -e --id Python.Python.3.12 --accept-package-agreements --accept-source-agreements
```

### 2.2 验证 Python 3.12 是否可用

```powershell
py -0p
py -3.12 -c "import sys; print(sys.version)"
```

看到 `-V:3.12` 即表示可用。

---

## 3. 创建虚拟环境（隔离依赖）

虚拟环境用于将项目依赖安装在本项目目录内，避免污染系统 Python。

在 `\amusement_stats` 目录下执行：

```powershell
py -3.12 -m venv venv
```

说明：

- 这会创建 `venv/` 目录，其中包含独立的 Python 与 pip。
- 后续命令都建议使用 `.\venv\Scripts\python.exe` 来确保使用的是虚拟环境解释器。

---

## 4. 安装依赖（两种模式）

本项目依赖分为两类：

- **核心依赖（启动必需）**：Django、asgiref、sqlparse、tzdata、openpyxl、et_xmlfile、Pillow 等
- **可选依赖（增强功能）**：folium、plotly（游客端地图/图表）

### 4.1 联网安装（完整功能，推荐）

```powershell
.\venv\Scripts\python.exe -m pip install -r requirements.txt
```

说明：

- 会安装 `requirements.txt` 中列出的全部依赖（包含 folium/plotly）。
- 适合开发与演示环境，游客端“数据洞察/园区地图”也会正常工作。

### 4.2 离线安装（仅使用 offline_packages）

如果机器没有外网，可以先尝试用离线 wheel 安装核心依赖：

```powershell
.\venv\Scripts\python.exe -m pip install --no-index --find-links=offline_packages ^
  asgiref==3.11.1 Django==6.0.4 sqlparse==0.5.5 tzdata==2026.1 et_xmlfile==2.0.0 openpyxl==3.1.5
```

重要说明（务必阅读）：

- 当前仓库的 `offline_packages/` **不包含** `folium/plotly/Pillow` 的 wheel。
  - 这意味着：若你希望离线环境下也能完整安装 `requirements.txt`，需要你提前准备这些包对应版本的 wheel 并放入 `offline_packages/`。
- 其中 **Pillow** 是 `ImageField`（项目封面上传）相关依赖，缺少时可能影响后台上传/校验图片。
- `folium/plotly` 缺少时：
  - 游客端“数据洞察/园区地图”页面会降级提示缺依赖
  - 其他功能仍可正常访问（该项目对这两项做了惰性导入与降级处理）

### 4.3 快速验证依赖是否装齐

```powershell
.\venv\Scripts\python.exe -c "import django; import openpyxl; import PIL; print('ok', django.get_version())"
```

能输出 `ok` 且无异常，表示核心依赖已满足启动条件。

---

## 5. 初始化数据库（迁移）

本项目默认使用 SQLite（`db.sqlite3`）。首次启动或模型变更后，需要执行迁移：

```powershell
.\venv\Scripts\python.exe manage.py migrate
```

说明：

- `migrate` 会根据各 app 的 `migrations/` 脚本创建/更新表结构
- 在你首次拉代码运行时，这是必做步骤

---

## 6. 创建管理员账号（用于后台登录）

后台登录需要可用账号，管理员建议用 Django 的超级管理员（superuser）：

```powershell
.\venv\Scripts\python.exe manage.py createsuperuser
```

说明：

- 该命令是交互式的，会要求输入用户名/密码
- 创建完成后，可使用该账号登录后台并管理用户/角色/项目等

---

## 7. 初始化角色组与演示数据（可选但强烈建议用于演示）

### 7.1 初始化角色组（admin / staff）

```powershell
.\venv\Scripts\python.exe manage.py init_rbac
```

如需把某个用户直接加入角色组，可用：

```powershell
.\venv\Scripts\python.exe manage.py init_rbac --admin <用户名>
.\venv\Scripts\python.exe manage.py init_rbac --staff <用户名>
```

### 7.2 生成演示数据（便于看板图表有数据）

```powershell
.\venv\Scripts\python.exe manage.py seed_demo_data --days 14 --records-per-day 40
```

说明：

- 会创建一批项目与游玩记录
- 适用于答辩演示：看板图表、排行、预测与预警等模块能立即看到效果

---

## 8. 启动服务

开发环境启动命令：

```powershell
.\venv\Scripts\python.exe manage.py runserver 127.0.0.1:8000
```

看到类似输出即表示启动成功：

- `Starting development server at http://127.0.0.1:8000/`

停止服务：

- 在该命令窗口按 `Ctrl + Break`（Windows）结束服务

---

## 9. 常用访问地址（启动后在浏览器打开）

- 运营看板首页：http://127.0.0.1:8000/
- 工作人员登录页：http://127.0.0.1:8000/login/
- Django 管理后台：http://127.0.0.1:8000/admin/
- 游客端首页：http://127.0.0.1:8000/visitor/

---

## 10. 常见问题排查

### 10.1 报错：Django requires Python >= 3.12

原因：

- 当前 Python 版本低于 3.12，但 `requirements.txt` 固定了 `Django==6.0.4`

处理：

- 安装 Python 3.12+ 并用 `py -3.12 -m venv venv` 重新创建虚拟环境

### 10.2 页面提示缺少 plotly/folium

原因：

- 游客端“数据洞察/园区地图”依赖 `plotly/folium`

处理：

- 联网环境：重新执行 `pip install -r requirements.txt`
- 离线环境：准备对应 wheel 并放入 `offline_packages/` 后再安装

### 10.3 后台上传封面时报 Pillow 相关错误

原因：

- Pillow 未安装或版本异常

处理：

- 联网环境：`pip install Pillow==11.1.0`
- 离线环境：准备 Pillow 的 wheel 包并通过 `--find-links` 安装

