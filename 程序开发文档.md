# 游乐场热门项目统计平台——程序开发文档

## 1. 文档目的与范围

本文档用于说明“基于 Python 的游乐场热门项目统计平台（Web 版）”的实现目标、功能范围、技术选型、系统架构、数据设计、核心业务流程与开发线路（实施路线）。适用于毕业设计开发、验收与答辩陈述。

> 说明：本项目定位为**中小型游乐场**的低成本数字化统计平台，强调“易用、可维护、可扩展”。在实现时允许将部分“可选/创新功能”以迭代方式落地（先实现基础统计与看板，再逐步加入预测与推荐）。

---

## 2. 项目概述

### 2.1 背景痛点

- 传统人工纸质记录/手工统计：数据更新不及时、难追溯、易出错
- 数据分散形成“数据孤岛”：无法形成统一报表与经营看板
- 缺乏面向运营决策的统计维度：只看人次不够，缺少排队时长、重复游玩等指标

### 2.2 建设目标

- 提供**易上手的数据录入**能力，让一线员工快速记录项目游玩数据
- 通过后端统计分析生成：**热门项目排行**、**时段客流**等关键报表与可视化图表
- 对管理员提供：多维度指标分析与导出能力；可逐步扩展**预测与预警**
- 通过权限控制（RBAC）实现普通用户与管理员功能隔离，保障数据安全

---

## 3. 主要功能设计（按角色）

### 3.1 普通用户（工作人员）

- **登录/退出**
  - 使用账号密码登录系统
  - 仅能访问其授权功能页面
- **游玩数据录入**
  - 填写游客游玩记录：项目、游玩时间、排队时长、是否重复游玩/重复次数等
  - 支持项目运行状态维护：正常/维护/关停（保证统计口径真实）
- **数据校验与容错**
  - 必填项校验（项目、时间等）
  - 数值范围校验（排队时长、次数）
  - 非法字符/重复数据提示与拦截

### 3.2 管理员（运营/管理人员）

- **项目管理**
  - 项目元数据维护：名称、类型、容纳人数（承载力阈值）、状态等
- **经营看板（统计分析 + 可视化）**
  - 热门项目排行（按游玩人次、或按综合热度分）
  - 时段客流分析（小时/半天/全天）
  - 项目类型占比（可选）
  - 区域/项目热度热力图（可选扩展）
- **多维指标分析（可迭代扩展）**
  - 排队时长统计、波动趋势
  - 重复游玩率、周转率等指标
  - 日/周/月维度回溯对比，识别热度衰减周期
- **报表导出**
  - 一键导出 Excel/CSV（统计结果与图表数据）
  - 周报/简报输出（可选）
- **预测与预警（可选/创新）**
  - 基于历史序列的客流/热度预测（滑动平均/回归，或 Prophet/LSTM）
  - 承载力阈值预警：预测触线时展示红色预警，辅助分流
- **权限与审计（RBAC）**
  - 用户/角色管理：普通用户与管理员
  - 权限隔离：敏感数据、系统参数仅管理员可见/可改

---

## 4. 技术选型与理由

### 4.1 后端

- **Python 3.x**
  - 生态成熟、数据处理便利、学习成本低
- **Django（建议 3.2 LTS 或更高）**
  - 自带 ORM、认证与权限体系，开发效率高
  - 适合快速搭建管理后台与表单录入系统

### 4.2 数据与分析

- **MySQL 5.7（或更高）**
  - 关系型结构清晰，利于按项目、时间维度统计与关联
- **Pandas**
  - 数据清洗与聚合统计高效：去重、缺失值处理、groupby、resample 等

### 4.3 前端与可视化

- **HTML5 + CSS3 + Bootstrap**
  - 响应式页面，适配电脑/平板/手机；降低交互门槛
- **ECharts**
  - 适合做柱状图、折线图、饼图、热力图等经营看板
- **Ajax（异步请求）**
  - 不刷新页面更新图表数据，支持定时刷新（如 5 分钟）

### 4.4 预测与推荐（可选）

- **Prophet / LSTM（迭代实现）**
  - Prophet：上手快、解释性较好，适合短期预测
  - LSTM：适合复杂时序，但训练与数据要求更高

---

## 5. 系统架构与模块划分

### 5.1 总体架构（MVC 思路）

- **表现层（View）**：模板页面 + Bootstrap，提供录入表单与图表展示页  
- **业务层（Controller）**：Django Views/Services，处理请求、校验、统计计算  
- **数据层（Model）**：Django ORM + MySQL，定义数据模型与关系

### 5.2 模块划分建议

- **用户与权限模块**
  - 登录、退出、用户管理、角色/权限配置（RBAC）
- **项目管理模块**
  - 项目元数据维护、运行状态维护、承载力阈值配置
- **数据采集模块**
  - 游玩记录录入、批量导入（后续扩展）
- **数据处理模块**
  - 清洗：去重、缺失值、时间格式统一
  - 统计：人次、排队时长、重复游玩率、周转率、综合热度评分（可选）
- **可视化与报表模块**
  - ECharts 图表页、Ajax 数据接口、导出
- **预测与预警模块（可选）**
  - 模型训练/预测、误差评估、阈值预警逻辑

---

## 6. 数据库设计（建议表结构）

> 下述为“建议字段”，实现时可根据实际页面与统计口径增删。

### 6.1 游乐项目表 `amusement_project`

- **project_id**：主键
- **name**：项目名称
- **type**：项目类型（亲子/刺激/观光等）
- **capacity**：容纳人数/承载阈值
- **status**：运行状态（normal/maintenance/closed）
- **created_at / updated_at**

### 6.2 游玩记录表 `play_record`

- **record_id**：主键
- **project_id**：外键 -> `amusement_project`
- **play_time**：游玩时间（datetime）
- **queue_time**：排队时长（分钟，int，可为空）
- **is_repeat**：是否重复游玩（bool）
- **repeat_count**：重复次数（int，可为空或默认 0）
- **source**：数据来源（manual/import，可选）

### 6.3 系统用户表 `sys_user`（或直接用 Django User + Profile）

推荐优先使用 Django 内置 `auth_user`、`auth_group`、`auth_permission` 体系实现 RBAC；如需扩展再加 Profile 表。

若采用 Django 内置：

- **用户**：`auth_user`（用户名、密码哈希、是否启用等）
- **角色/组**：`auth_group`
- **权限**：`auth_permission`
- **用户-组关联**：`auth_user_groups`

若自建 `sys_user`（不推荐，但可用于课程作业简化）：

- **user_id**：主键
- **username**：用户名（唯一）
- **password_hash**：密码哈希（禁止明文）
- **role**：角色（staff/admin）
- **is_active**：是否启用
- **created_at / updated_at**

---

## 7. 核心业务流程（实现线路对应）

### 7.1 数据录入流程（工作人员）

1. 用户登录进入“数据录入”页面  
2. 选择项目、填写游玩时间、排队时长、重复游玩信息等  
3. 后端进行字段校验与合法性校验  
4. 写入 `play_record` 表（必要时做“疑似重复”提示）  
5. 返回录入成功提示；看板页通过 Ajax 拉取最新统计数据刷新图表（可选定时刷新）

### 7.2 热门项目排行（基础版）

- 统计口径：按时间范围（默认当天/本周/自定义）对 `play_record` 以 `project_id` 分组，计数人次（或记录数）
- 输出字段：项目名称、游玩人次、占比、排名
- 可选增强：按“综合热度分”排序（见 7.4）

### 7.3 时段客流分析（基础版）

- 统计口径：对 `play_time` 按小时/半天/天聚合
- 输出字段：时间桶、客流量（人次）
- 展示方式：折线图 + 峰值标记；支持筛选日期范围

### 7.4 多维热度评分（增强/可选）

可采用“可解释、易实现”的加权评分，先跑通业务再逐步加特征：

- 基础指标：游玩人次（高越好）、平均排队时长（过高可能表示拥挤，需结合承载力）
- 用户维度：重复游玩率（高说明粘性强）
- 运营维度：维护/关停时长影响（高则扣分）
- 外部维度：节假日/天气（可后续迭代接入）

示例（可调整）：

\[
Score = w_1 \cdot norm(plays) + w_2 \cdot norm(repeat\_rate) - w_3 \cdot norm(downtime) + w_4 \cdot norm(service\_efficiency)
\]

其中 `service_efficiency` 可用（单位时间周转量）或（排队时长的反向指标）近似。

---

## 8. 页面与接口清单（建议）

### 8.1 页面（模板页）

- **登录页**：`/login/`
- **数据录入页**（工作人员）：`/records/new/`
- **项目列表/维护**（管理员）：`/projects/`
- **看板总览**（管理员）：`/dashboard/`
- **报表导出**（管理员）：`/export/`
- **用户与权限管理**（管理员，可选）：`/admin/` 或自建 `/users/`

### 8.2 数据接口（Ajax/JSON）

- **热门排行数据**：`/api/rank?start=...&end=...`
- **时段客流数据**：`/api/traffic?start=...&end=...&bucket=hour|day`
- **项目类型占比**（可选）：`/api/type_ratio?start=...&end=...`
- **预测结果**（可选）：`/api/predict?days=7`

接口返回建议统一：

- `code`：0 成功，非 0 失败
- `message`：提示信息
- `data`：图表所需数组/对象

---

## 9. 关键实现要点（落地建议）

### 9.1 数据清洗（Pandas）

- **去重**：对（项目 + 游玩时间 + 录入人/来源）等组合键做去重策略
- **缺失值**：排队时长缺失时可用均值/中位数填补，或在图表中单独统计“缺失比例”
- **时间统一**：统一时区/格式；将字符串转 `datetime`

### 9.2 统计计算（Pandas / ORM）

- 小数据量：可直接 ORM 聚合（如 `annotate(Count(...))`）
- 统计维度多、逻辑复杂：可从 ORM 拉取到 DataFrame 再处理

建议开发顺序：

- 先用 ORM 跑通统计（上线快）
- 再按需要引入 Pandas 实现更复杂指标（更灵活）

### 9.3 可视化（ECharts）

- 热门排行：柱状图（项目名 -> 人次）
- 时段客流：折线图（时间桶 -> 人次）
- 类型占比：饼图（可选）
- 交互：hover 显示数值、日期筛选、图表缩放

### 9.4 权限控制（RBAC）

- 工作人员：仅能录入与查看自己需要的局部页面
- 管理员：项目维护、看板、导出、用户管理
- Django 落地方式：
  - 用 Group 区分角色（staff/admin）
  - 视图层使用 `@login_required` 与权限装饰器/中间件控制访问

---

## 10. 开发线路（实施路线/里程碑）

### 10.1 环境准备

- Windows + Python + MySQL
- 创建 Django 项目与应用（建议按模块拆 app）
- 配置数据库连接、静态文件、模板目录

### 10.2 需求与原型（第 1 阶段）

- 明确录入字段与统计口径（人次、排队时长、重复游玩等）
- 画 3 个核心页面草图：登录、录入、看板

### 10.3 核心功能开发（第 2 阶段）

- 用户登录与权限（最小可用）
- 项目表与项目管理（管理员）
- 游玩记录录入（工作人员）

### 10.4 统计与看板（第 3 阶段）

- 热门排行统计 + ECharts
- 时段客流统计 + ECharts
- Ajax 接口与定时刷新（可选）

### 10.5 报表与导出（第 4 阶段）

- 导出 CSV/Excel
- 运营周报模板（可选）

### 10.6 增强与创新（第 5 阶段，可选）

- 多维热度评分
- 预测（Prophet/滑动平均/回归；LSTM 视数据量决定）
- 承载力阈值预警
- 个性化推荐（避峰/路线/组合）

### 10.7 测试与优化（第 6 阶段）

- 功能测试、容错测试、权限测试
- 易用性测试：录入流程尽量 3 步内完成
- 性能体验：页面加载与图表渲染无明显卡顿

---

## 11. 运行与部署说明（建议写法）

### 11.1 本地运行（开发）

- 安装依赖（Python、Django、数据库驱动等）
- 初始化数据库（迁移、创建管理员账号）
- 启动服务并访问本地地址（例如 `http://127.0.0.1:8000/`）

### 11.2 数据初始化

- 先录入项目基础数据（项目名称、类型、承载阈值）
- 再由工作人员开始录入游玩记录
- 如有历史数据，可提供 CSV 导入（后续迭代）

---

## 12. 测试与验收指标

### 12.1 测试类型

- **功能测试**：录入、统计、图表显示、导出、登录权限
- **易用性测试**：非技术用户能否快速完成录入与查看
- **数据容错测试**：非法字符、空值、重复数据、超范围数值
- **权限测试**：普通用户无法访问管理员页面与敏感数据

### 12.2 验收标准（可用于答辩）

- 统计结果与原始数据一致（关键口径正确）
- 核心流程可用：登录 → 录入 → 看板展示 → 导出
- 操作步骤简洁，页面布局清晰
- 具备基础安全性：密码不明文、权限隔离有效

---

## 13. 可扩展方向（后续优化）

- 对接门票系统/闸机数据，减少人工录入
- 增加更多指标：投诉率、评分、设备故障率等
- 更精细预测：分工作日/周末/节假日模型
- 地图可视化：项目热度辐射与分流路径建议









