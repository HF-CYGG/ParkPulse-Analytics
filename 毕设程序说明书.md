# 游乐场热门项目统计平台 · 程序说明书

## 一、项目概述

本系统基于 Django 构建，面向游乐场运营侧提供游玩记录管理、项目与看板统计、权限与审计，并向游客提供项目浏览、人气参考、数据洞察图表、园区地图与天气提示等功能。数据持久化使用关系型数据库，静态资源与上传文件分离配置。

---

## 二、主要技术

| 类别 | 技术 |
| --- | --- |
| 后端框架 | Django 6 |
| 数据库 | SQLite 或兼容 Django 的关系库 |
| 前端 | Bootstrap 5、原生 JavaScript、ECharts、Plotly、Leaflet、Folium |
| 其他 | Pillow 图片、openpyxl 表格导入、Open-Meteo 天气接口免密钥 |

---

## 三、目录树与文件说明

根目录 `程序` 下以 `amusement_stats` 为主工程文件夹。

```
程序/
├── README_部署说明.md          部署与运行说明，可选阅读
├── 毕设程序说明书.md            本文件
└── amusement_stats/
    ├── manage.py               Django 命令行入口，迁移与启动服务
    ├── requirements.txt        Python 依赖版本清单
    ├── amusement_stats/        项目配置包
    │   ├── settings.py         数据库、应用注册、静态与媒体路径、中间件等
    │   ├── urls.py             根路由，挂载各应用与登录登出
    │   ├── wsgi.py             WSGI 部署入口
    │   ├── asgi.py             ASGI 异步部署入口
    │   ├── templates/          全局与各模块 HTML 模板
    │   └── static/             全局 CSS、JS、第三方库与图片
    ├── accounts/               账号与工作人员登录相关视图与路由
    ├── core/                   系统设置、审计日志、上下文处理器、管理命令
    ├── dashboard/              运营看板、图表与热度聚合逻辑
    ├── records/                游玩记录模型、表单、增删改查与 Excel 导入
    ├── projects/               游乐项目模型、列表与编辑、封面与坐标、地图默认坐标
    ├── visitor/                游客端页面、天气、Plotly 与 Folium 封装、收藏与反馈
    └── media/                  运行时用户上传目录，如项目封面，由 settings 指定
```

### 各应用内重点文件

**amusement_stats 配置包**

- `settings.py`：注册 `INSTALLED_APPS`、模板与静态目录、`MEDIA` 上传、`DEBUG` 等。
- `urls.py`：总路由；开发模式下挂载媒体文件访问。

**accounts**

- `views.py`：工作人员登录等视图。
- `forms.py`：登录相关表单。
- `urls.py`：账号相关路径。

**core**

- `models.py`：系统配置键值等扩展模型。
- `views.py`：系统设置页、审计与游客反馈管理后台视图。
- `audit.py`：记录管理员关键操作日志。
- `auth_utils.py`：管理员与工作人员权限判断辅助函数。
- `context_processors.py`：向模板注入侧边栏所需角色等全局变量。
- `urls.py`：系统页与审计路由。
- `management/commands/init_rbac.py`：初始化角色权限。
- `management/commands/seed_demo_data.py`：演示数据种子，可选。

**dashboard**

- `views.py`：首页看板、ECharts 数据接口、区域热力与周报等聚合。
- `urls.py`：看板与报表路径。

**records**

- `models.py`：`PlayRecord` 游玩记录字段，关联项目与用户。
- `forms.py`：录入与编辑表单。
- `views.py`：新建、编辑、删除、Excel 导入导出逻辑。
- `urls.py`：记录模块路由。

**projects**

- `models.py`：`Project` 项目类型、状态、区域、封面、经纬度等。
- `forms.py`：`ProjectCreateForm` 列表快速新建，`ProjectForm` 完整编辑。
- `views.py`：项目列表、编辑、删除、状态切换与地图初始坐标计算。
- `location_defaults.py`：各园区分区参考经纬度，供编辑页地图与游客端示意点复用。
- `urls.py`：项目管理路由。
- `migrations/`：数据库结构演进脚本。

**visitor**

- `models.py`：游客收藏与反馈模型。
- `forms.py`：反馈表单。
- `views.py`：首页、项目列表与详情、账户、收藏、反馈、数据洞察与园区地图视图。
- `services.py`：对外可展示的人气档位聚合，隐藏精确人次。
- `weather.py`：调用 Open-Meteo 拉取当前天气并生成中文提示文案。
- `explore_charts.py`：Plotly 游玩记录按日与按小时统计、Folium 地图 HTML 生成。
- `urls.py`：游客端路径与公开 JSON 接口。
- `migrations/`：游客相关表结构。

**templates 与 static 说明**

- `templates/base.html`：运营后台整体布局与侧栏。
- `templates/dashboard/`：看板与周报页面。
- `templates/records/`：游玩记录各操作页。
- `templates/projects/`：项目列表、编辑含 Leaflet 选点、删除确认。
- `templates/visitor/`：游客端基类导航、首页、列表、详情、数据洞察、地图、账户与反馈等。
- `templates/registration/login.html`：登录页。
- `static/css/app.css` 与 `visitor.css`：后台与游客端样式。
- `static/js/app.js`：主题与通用脚本。
- `static/vendor/`：Bootstrap、ECharts 等第三方静态文件。

---

## 四、功能模块划分

1. **身份与权限**：登录登出、管理员与工作人员区分、部分页面仅管理员可见。
2. **游乐项目管理**：增删改查、状态切换、封面与简介、经纬度表单与地图选点。
3. **游玩记录管理**：单条录入与编辑、批量 Excel 导入、与项目外键关联。
4. **运营看板**：ECharts 图表、按时间与区域的热度或统计展示。
5. **审计与系统配置**：操作日志查询、系统参数与反馈处理等后台页。
6. **游客端**：项目列表卡片与详情、近七日人气档位、天气条、Plotly 真实聚合图表、Folium 园区地图、收藏与意见反馈。

---

## 五、实现方法与要点

- **ORM 与迁移**：模型变更通过 `makemigrations` 与 `migrate` 同步数据库。
- **表单与文件上传**：编辑项目使用 `multipart` 与 `request.FILES` 保存封面至 `MEDIA_ROOT`。
- **聚合统计**：游玩记录用 `TruncDate`、`Extract` 与 `Count` 做按日、按小时聚合；看板侧另有分位与档位化逻辑保护敏感数据。
- **游客热度**：`visitor/services.py` 对原始人次做分档，列表仅展示高、中、低等档位描述。
- **天气**：`urllib` 请求 Open-Meteo 当前要素，按温度与降水码生成中文温馨提示。
- **Plotly**：服务端生成图表 HTML 片段，模板中 `safe` 渲染，并加载中文 locale 脚本。
- **Folium**：服务端生成地图 HTML；默认高德路网图层，可与 OSM 切换。
- **地图选点**：编辑页用 Leaflet 与高德瓦片，拖动或点击标记回写经纬度输入框，并与 `location_defaults` 中区域参考点联动按钮。

---

## 六、关键实现要点摘录

**惰性导入图表模块**  
在游客数据洞察视图中于函数内部再 import 图表生成函数，并用 try 捕获模块缺失异常，这样未安装 Plotly 时仅该页降级提示，其余路由仍可加载。

**游玩记录按日聚合**  
对 `PlayRecord` 查询集按游玩时间下限过滤后，使用按日期截断字段、再按日期分组计数、排序，得到近若干日每日条数序列，供 Plotly 绑制柱状图横轴与纵轴。

**编辑页地图与输入框**  
页面脚本用 Leaflet 创建可拖动标记，监听拖动结束与地图单击事件，将得到的纬度经度写回表单里纬度经度两个输入框的 value；同时监听输入框变更，把标记平移到新坐标，实现双向一致。

**媒体与封面**  
`settings` 中配置 `MEDIA_URL` 与 `MEDIA_ROOT`，编辑表单 `enctype` 设为支持文件上传，视图中传入 `request.FILES` 与表单一并校验保存，`ImageField` 存相对路径，模板里用 `cover_image.url` 输出访问地址。

---

## 七、答辩常见问题与简短参考答法

**问：系统总体架构是什么？**  
答：浏览器通过 Django 视图与模板交互，业务按应用划分，数据存关系型数据库，静态与上传文件由 Web 服务器或开发环境分别托管，游客端与运营端共用一套后端、不同模板与权限。

**问：游玩记录如何保证与项目对应？**  
答：外键关联 `Project` 模型，删除策略由模型定义，录入时必选或必选关联项目，列表与统计按该外键分组或筛选。

**问：游客看到的人气为何不是具体人数？**  
答：出于经营数据隐私，服务层对近若干日人次做分位或档位映射，只返回档位与文案，不暴露精确值。

**问：数据洞察里的图表数据是否真实？**  
答：来自 `PlayRecord` 表真实入库记录，按游玩时间字段做近十四日按日与近七日按小时计数聚合，非随机模拟。

**问：天气数据从哪来、代表哪里？**  
答：使用 Open-Meteo 公开接口，默认经纬度在 `weather.py` 中配置，代表该坐标附近格点预报，时区为上海时区；部署时可改为乐园所在地经纬度。

**问：地图经纬度不准怎么办？**  
答：无坐标时系统用分区参考点加哈希偏移示意；后台编辑页支持地图点选与区域参考按钮，保存 WGS84 经纬度；若与高德拾取一致需注意坐标系说明。

**问：用了哪些可视化库、各负责什么？**  
答：ECharts 用于运营看板；Plotly 用于游客端可交互折线与柱状图；Leaflet 用于后台选点；Folium 用于服务端生成游客端园区地图 HTML。

**问：如何保证管理员操作可追溯？**  
答：`core` 中审计模块在关键写操作后写入日志，后台提供日志列表查询。

---

文档版本：与当前仓库功能对应，以后台与 `visitor` 应用源码为准。
